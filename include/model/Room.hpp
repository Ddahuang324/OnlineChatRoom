#pragma once

// Placeholder for the Room entity as per T011.
// struct Room { std::string id; std::string name; bool isPublic; };
// Implementation will be provided by the user.
#include <string>

struct Room {
    std::string id;
    std::string name;
    bool isPublic;
};