// Placeholder for ChatPage.qml as per T045.
// Elements: ListView(messages), TextArea(input), Button(send), Button(sendFile)
// Implementation will be provided by the user.
import QtQuick 2.15

Item {
}
